{
  "private": true,
  "packageManager": "yarn@4.5.3",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start": "yarn workspace stencil-components start",
    "build": "yarn workspace stencil-components build && yarn workspace react-library build",
    "storybook": "yarn workspace stencil-components build && yarn workspace react-library storybook",
    "build-storybook": "yarn workspace react-library build-storybook"
  },
  "resolutions": {
    "path-scurry": "^2.0.0"
  }
}
